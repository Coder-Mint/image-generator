<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Generator</title>
    <!-- Google Fonts: -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito&display=swap" rel="stylesheet">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <!-- Link to the CSS File -->
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
    <div>
        <form id="myForm" method="post">  <!-- Create a form that performs POST requests to the server. -->
            <!-- Search Query Input -->
            <label for="search">Search:</label>
            <input type="text" id="search" name="search">
            
            <input type="submit" class="btn btn-success" value="Random" formaction="/random">  <!-- Make a button that, on click, calls the '/random' endpoint. -->
            <input type="submit" class="btn btn-primary" value="Search" formaction="/search">  <!-- Make a button that, on click, calls the '/search' endpoint. -->
        </form>

        <% if (locals.url) { %>  <!-- If the 'url' variable was passed from the JavaScript code... -->
            <img src="<%= url %>" alt="<%= desc %>" class="main-img">  <!-- Create an image whose source is the 'url' variable's value (i.e the link of photo)  -->
            <!-- Create a button that redirects to the URL itself, in other words, it opens the image in another tab (If you know a way to make this download the image, please email me at saad.waqas.jeddah@gmail.com ) -->
            <a href="<%= url %>" class="download">
                <button type="button" class="btn btn-outline-dark">Download</button>
            </a>
        <% } else { %>  <!-- If the 'url' variable wasn't specified... -->
            <% if (locals.error) { %>  <!-- If the 'error' variable was passed from the JavaScript code... -->
                <p class="error-msg"><%= error %></p>   <!-- Show the error as text. -->
            <% } else { %>  <!-- Otherwise... (if neither the 'url' variable nor the 'error' variable were specified)  -->
                <p>Waiting for data...</p>  <!-- Just show "Waiting for data..." -->
            <% } %>
        <% } %>
    </div>
    
</body>
</html>